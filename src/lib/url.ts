// URL normalization helpers shared by capture + CSV ingestion.
// Implementation lands with Phase 2. Contract: input may be a bare hostname
// or full URL; output is an absolute https:// URL or `null` if invalid.

export {};
